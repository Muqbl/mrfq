# XSS and `innerHTML` Review

Date: 2026-06-19  
Scope: first-party files in `public/app.js` and `public/js/`. The vendored QR library is excluded from first-party remediation and remains constrained to self-hosted script execution.

The scan found 62 first-party writes and 6 read/clear checks. Every occurrence is represented below; identical renderer patterns are grouped with all line numbers. `escapeHtml()` is the shared first-party helper and `esc` remains its compatibility alias.

| File | Line/Function | Source of HTML | User-controlled data? | Escaped? | Status | Fix |
|---|---|---|---|---|---|---|
| `public/app.js` | 715 `toast` | Static icon only | No | N/A | SAFE_STATIC_TEMPLATE | Message moved to `textContent` |
| `public/app.js` | 854,857,873,882,4053,5969 reads | Form-presence checks | No write | N/A | SAFE_STATIC_TEMPLATE | Read-only; no change |
| `public/app.js` | 958-959,1311 | SVG icon helper | No | N/A | SAFE_STATIC_TEMPLATE | Self-owned SVG registry |
| `public/app.js` | 1067 `showModal` | Trusted renderer fragments | Indirect | Leaf renderers required to escape | LEGACY_REVIEWED | Replace fragment contract with DOM components in future extraction |
| `public/app.js` | 1343,1370,1408,1439,1473 | Login/core shells | Authentication labels/state | Static translations | LEGACY_REVIEWED | Renderer-owned templates; no raw credential echo |
| `public/app.js` | 1701,1709,1730,1840 | Admin/facility shells | API records | Leaf values use `esc` | SAFE_ESCAPED_TEMPLATE | Retain leaf escaping during modularization |
| `public/app.js` | 2722 notifications | Tickets/orders/assets | Yes | Labels/subtitles escaped before composition | SAFE_ESCAPED_TEMPLATE | Inline actions still tracked under CSP migration |
| `public/app.js` | 2858 gallery | Server/local image URLs | Bounded path/data URL | Source restricted by CSP and upload rules | LEGACY_REVIEWED | Replace generated handlers and validate URL at component boundary |
| `public/app.js` | 3065 comments empty state | Static | No | N/A | SAFE_STATIC_TEMPLATE | Move presentation styles to CSS later |
| `public/app.js` | 3069 comments list | Comment body/name/role/time | Yes | `esc` applied to name, role and body; time formatted | SAFE_ESCAPED_TEMPLATE | Role fallback escaping added in Phase 2 |
| `public/app.js` | 3337,3390 activity timelines | API events and actor names | Yes | Timeline labels pass through `esc` | SAFE_ESCAPED_TEMPLATE | None |
| `public/app.js` | 3890 role cards | Fixed role registry | No | N/A | SAFE_STATIC_TEMPLATE | None |
| `public/app.js` | 4022,4498,4947,5318,5347,5379,5401,5410,5780,6888,7010 | Page/shell renderers | Mixed operational data | Leaf renderer escaping | LEGACY_REVIEWED | Continue component extraction; scanner guards direct raw errors/messages |
| `public/app.js` | 4034,4328 | Camera/QR overlays | Static labels and library container | No raw user text | SAFE_STATIC_TEMPLATE | None |
| `public/app.js` | 4054,5970 | Form clear | No | N/A | SAFE_STATIC_TEMPLATE | Empty assignment only |
| `public/app.js` | 4097,4459 worker forms | Ticket/location data | Yes | Operational fields use `esc`/safe select helpers | SAFE_ESCAPED_TEMPLATE | Preserve helper use |
| `public/app.js` | 4193 | Camera mode label | Translation enum | No | Static translation | SAFE_STATIC_TEMPLATE | None |
| `public/app.js` | 4234,4271,4279 | Photo previews | Local data URLs/server-generated paths | Bounded | CSP/upload validation | LEGACY_REVIEWED | Move to DOM image builder and generated listeners |
| `public/app.js` | 4439,4472,4743,4766,4785,4879,4902,6028,6061 | Button states | Static icon/translation/count | No free text | N/A | SAFE_STATIC_TEMPLATE | Spinner inline styles remain CSP work |
| `public/app.js` | 4771,4890 | Completion panels | Returned reference and static labels | Yes | Reference values escaped by renderer | SAFE_ESCAPED_TEMPLATE | None |
| `public/app.js` | 5075-5076 hospitality activity | API events/actors or static error | Yes | Event and actor values use `esc`; error text static | SAFE_ESCAPED_TEMPLATE | None |
| `public/app.js` | 5986,6051 maintenance forms/results | Work-order data | Yes | Leaf values use `esc`/safe controls | SAFE_ESCAPED_TEMPLATE | Preserve helper use |
| `public/app.js` | 6267 checklist row | Static form controls | User enters after DOM creation | Not interpolated | SAFE_STATIC_TEMPLATE | Convert removal handler during CSP migration |
| `public/app.js` | 6391,6400 menu preview | Locally generated image data/static icon | Bounded | Data generated by image pipeline | LEGACY_REVIEWED | Build image node directly in future |
| `public/js/platform/facilities.js` | 12 facilities renderer | API facility/space data | Yes | Every string passes local `esc` | SAFE_ESCAPED_TEMPLATE | None |
| `public/js/platform/facilities.js` | 19 error state | Error message | Potentially | `esc(error.message)` | SAFE_ESCAPED_TEMPLATE | None |

## Automated guard

`npm run test:security` inventories first-party writes, reads, inline handlers and inline styles. It fails on direct interpolation of raw `error.message`/`msg`, missing shared escaping, or regression of toast text handling. Final result: no detected direct raw-message sinks.

No first-party usage is currently classified `NEEDS_FIX`. `LEGACY_REVIEWED` means reviewed and bounded, but still scheduled for conversion away from string-based rendering; it does not mean the pattern is preferred for new code.

## Phase 3 revalidation

- Current count: 62 first-party writes and 6 read/clear checks, unchanged from Phase 2.
- Increase after event/style migration: none.
- Dangerous raw sinks found: none.
- User/API/DB values: reviewed leaf renderers continue to use `escapeHtml`/`esc`; the facilities error path remains escaped.
- Delegated action arguments: serialized as JSON and escaped before insertion into `data-*` attributes.
- Executable strings: the action dispatcher has an explicit allowlist and does not use `eval` or `Function`; the security scan now rejects either construct.
- Simple messages: toast continues to use `textContent`.
- `npm run test:security`: passed after Phase 3 migration with 62 writes, 6 reads, 115 matching inline-handler source lines, 91 inline styles, and zero failures.

The migration did not create a new XSS sink. Remaining `LEGACY_REVIEWED` string renderers still require the existing leaf-escaping discipline until component extraction is complete.
