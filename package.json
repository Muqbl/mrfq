{
  "name": "rega-facility-care-prototype",
  "version": "2.0.0",
  "description": "REGA Facility Care — Prototype for governance review. Not for production use.",
  "scripts": {
    "start": "node server.js",
    "seed":  "node scripts/seed-demo.js",
    "test":  "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
